/* api.js (config) — адрес сервера с базой контента (Payload + PostgreSQL).

   Пусто ("") — сайт работает как раньше: уроки берутся из файлов data/*.js,
   интернет и сервер не нужны (режим «статика», по умолчанию).

   Укажи адрес — сайт загрузит курсы, модули и уроки из Payload по REST:
     window.SITE_API = "http://127.0.0.1:3000";

   Прогресс ученика в обоих режимах считается по одному и тому же номеру урока
   (siteId), поэтому оценки и ответы не теряются при переключении.

   Админка контента в этом режиме — админка Payload: (адрес)/admin */
window.SITE_API = "";
