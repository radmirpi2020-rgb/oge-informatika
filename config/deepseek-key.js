/* deepseek-key.js — шаблон для ключа DeepSeek (этот файл коммитится).

   Рабочий ключ сюда НЕ вписывай: файл попадает в git, и ключ останется
   в истории репозитория навсегда. Для ключа есть отдельный файл:

       config/deepseek-key.local.js      ← сюда ключ, он в .gitignore

   Содержимое local-файла:

       window.DEEPSEEK_API_KEY = "sk-твой-ключ";

   В index.html local-файл подключён ПОСЛЕ шаблона, поэтому перекрывает его.
   Пока ключа нет — сайт работает в простом режиме: провожатый объясняет тему
   урока, показывает лайфхаки и типичные ошибки, но не тратит токены.

   На сервере ключ вообще не нужен в браузере: см. server/README.md, раздел
   «Провожатый: ключ DeepSeek только на сервере» и переменную DEEPSEEK_API_KEY.
   Подробнее — config/README.txt */
